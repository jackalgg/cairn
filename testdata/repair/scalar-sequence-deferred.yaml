# Still deferred: a dashless item is only restored inside a schema-CONFIRMED
# scalar sequence (args/command). Here `steps` is a non-Kubernetes key cairn
# doesn't model, so it can't confirm the sequence is scalar and leaves the
# dashless `deploy` alone rather than guess a boundary.
steps:
- build
- test
  deploy
